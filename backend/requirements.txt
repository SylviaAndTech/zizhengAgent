fastapi==0.115.0
uvicorn[standard]==0.30.6
openai==2.53.0  # langchain-openai==1.4.1 需要 openai>=2.45.0，这里跟着一起提了版本
httpx[socks]==0.27.2  # 兜底：即便代码里已经禁用了httpx读取本机代理，也避免环境里缺socksio时报错
requests==2.34.2  # langchain-community==0.4.2 需要 requests>=2.32.5，这里跟着一起提了版本
trafilatura==1.12.2
sqlalchemy==2.0.35
pymysql==1.1.1
cryptography==43.0.3
python-multipart==0.0.9
pydantic==2.9.2
python-dotenv==1.0.1
python-docx==1.1.2
pymupdf==1.28.0  # 中文PDF解析明显比pypdf更稳（少乱码/少丢字），原来用的pypdf已替换掉
# 扫描版PDF（没有文字层，整页是图片）走OCR兜底，需要系统装tesseract+中文语言包，pip装不了：
#   macOS: brew install tesseract tesseract-lang
#   Ubuntu/Debian: apt install tesseract-ocr tesseract-ocr-chi-sim
# 没装tesseract时，普通PDF（有文字层）完全不受影响，只有真正遇到扫描件才会报错提示装tesseract
networkx==3.3
pyvis==0.3.2
matplotlib==3.9.2

# 导出Word时把"适用课程举例"树状图（Mermaid，跟前端页面上看到的同一张图）也渲染成图片
# 嵌进文档——Mermaid本身只在浏览器里跑，Python没有原生渲染能力，这里用Playwright起一个
# 无头Chromium加载mermaid.js来渲染。pip装这一行只是装Python绑定，浏览器内核还需要额外
# 执行一次（只用做一次，不是每次启动都要跑）：
#   playwright install chromium
# 没执行这一步时Mermaid图渲染会静默跳过（mermaid_render.py会打warning日志），Word文档
# 里案例的"适用课程举例"仍然有文字版表格，不影响其余内容正常导出
playwright==1.62.0

# 知识库检索：LlamaIndex（分块/向量存取）+ ChromaDB 向量库
# 用OpenAI兼容协议的llm/embeddings（不是llama-index-llms-dashscope/llama-index-embeddings-dashscope
# 那两个包——它们请求硬编码打到阿里云自己的dashscope.aliyuncs.com，跟我们统一用的硅基流动endpoint
# 不是一个平台，拿硅基流动的key打过去必然401）
llama-index-core==0.14.23
llama-index-llms-openai==0.7.10
llama-index-llms-openai-like==0.7.2
llama-index-embeddings-openai==0.6.0
llama-index-vector-stores-chroma==0.5.5
chromadb==1.5.9

# AI助手agent：LangChain（工具调用循环，内部基于LangGraph）
langchain==1.3.14
langchain-openai==1.4.1
langchain-core==1.5.3
# 案例正文写作-评审循环（writer/judge/reviser三节点状态图）直接用LangGraph搭建状态机，
# 之前只是langchain的传递依赖、没有直接声明，这里改成显式pin（不需要额外pip install，
# 当前环境已经装了这个版本）
langgraph==1.2.10

# 案例↔知识点匹配：向量粗筛之外再加一路关键词检索（跟向量检索结果做RRF融合），
# 弥补"案例叙事体 vs 知识点技术条目"语域差异太大、纯向量相似度区分度弱的问题。
# 两个都是纯Python小库，没有编译依赖：jieba做中文分词，rank-bm25做BM25打分
jieba==0.42.1
rank-bm25==0.2.2

# 知识图谱：只用langchain_community.graphs.graph_document里的Node/Relationship/GraphDocument
# 这几个数据结构类（LLMGraphTransformer的输出schema），用来表示案例↔知识点这个已经确定的
# 结构化关系——不实际调用LLM去抽取关系（这层关系本来就在CaseKnowledgeMapping表里，
# 用LLM去"猜"一个已经确定的关系没有意义，纯粹是复用LangChain这套图数据结构的表示方式）。
# 注意：langchain-community这个包官方标注为"sunset中、不再积极维护"，但graph_document这个
# 子模块只是几个轻量的pydantic数据类，没有用到包里其他被废弃的重度集成，可以放心用。
langchain-community==0.4.2
